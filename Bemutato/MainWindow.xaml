<Window x:Class="VadaszDenes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VadaszDenes"
        mc:Ignorable="d"
        Title="Super Minecraft Wall-E in Astroneer 2 Ultra: Deluxe Edition + New Funky Mode"
        Height="800" Width="1200">

    <DockPanel LastChildFill="True">

        <!-- Alsó státuszpanel Borderrel a Padding miatt -->
        <Border DockPanel.Dock="Bottom" Background="#222" Padding="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="lblAkkumulator" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblIdo" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblPozicio" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblLepesek" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblAsvanyok" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Jobboldali napló + vezérlőpult panel -->
        <Border DockPanel.Dock="Right" Width="220" Background="#333" Padding="5">
            <StackPanel>
                <TextBlock Text="Begyűjtött ásványok:" Foreground="White" FontWeight="Bold"/>
                <TextBlock x:Name="AsvanySzoveg" Text="0" Foreground="White" Margin="0,0,0,10"/>

                <TextBlock Text="Eseménynapló:" Foreground="White" FontWeight="Bold"/>
                <ListBox x:Name="NaploLista" Height="600" Width="200"/>
            </StackPanel>
        </Border>

        <!-- Fő játékterület ScrollViewer-rel -->
        <ScrollViewer x:Name="scrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <Grid x:Name="grdJatekter" Background="Black" Width="10000" Height="10000">
                <Grid.LayoutTransform>
                    <ScaleTransform x:Name="st" ScaleX="1" ScaleY="1" />
                </Grid.LayoutTransform>

                <!-- Teszt szöveg a zoomhoz -->
                <TextBlock Text="Játéktér" Foreground="White" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Rejtett rover gomb, ha animálni akarod külön -->
                <Button x:Name="btnRover" Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="Hidden">
                    <Image Source="/Assetts/rover.png"/>
                </Button>
            </Grid>
        </ScrollViewer>

    </DockPanel>
</Window>