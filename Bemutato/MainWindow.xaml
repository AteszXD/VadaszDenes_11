<Window x:Class="VadaszDenes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VadaszDenes"
        mc:Ignorable="d"
        Title="Super Minecraft Wall-E in Astroneer 2 Ultra: Deluxe Edition + New Funky Mode" Height="450" Width="800">

    <DockPanel LastChildFill="True">
        <!-- Bottom status / control panel -->
        <Border DockPanel.Dock="Bottom" Background="#222" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left: optional title or left aligned info -->
                <TextBlock Text="Rover Status" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Grid.Column="0" />

                <!-- Centered sleek horizontal row of controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,0">
                    <TextBlock x:Name="lblBattery" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblTime" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblPosition" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblSteps" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblMinerals" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <Button x:Name="btnMine" Content="Mine (M / Space)" Click="BtnMine_Click" Height="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblStatus" Foreground="LightGreen" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main scrollable play area (fills remaining space) -->
        <ScrollViewer x:Name="scrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <Grid x:Name="grdJatekter" Background="Black" Width="10000" Height="10000">
                <Grid.LayoutTransform>
                    <!-- Ez a transform felel a skálázásért -->
                    <ScaleTransform x:Name="st" ScaleX="1" ScaleY="1" />
                </Grid.LayoutTransform>

                <!-- Csak teszt jelleggel egy szöveg, hogy lásd a zoomot -->
                <TextBlock Text="Játéktér" Foreground="White" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="btnRover" Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="Hidden">
                    <Image Source="/Assetts/rover.png"/>
                </Button>
            </Grid>
        </ScrollViewer>
    </DockPanel>

</Window>